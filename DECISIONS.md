T1 | Added a minimal persisted auth layer with username/password and session token while keeping the diagnosis path working without a full external auth provider. Rejected: full OAuth or JWT infrastructure. Why: the project has no identity provider and the demo must continue without credentials. How to reverse: remove AppUser, AuthService, AuthController, and the session-guard calls in the profile/history flow.
T2 | Chose a lightweight farm/field model with farmer-owned records and district association, rather than building a geospatial map or multi-tenant schema. Rejected: complex GIS and per-field sensor schemas. Why: the feature requires per-farmer field tracking without slowing the demo or inventing unsupported infrastructure. How to reverse: delete Farm and Field entities and their repository/service wiring.
T3 | Chose a feedback entity that stores farmer label, diagnosis, model version, and timestamp for later review/export. Rejected: model retraining pipeline or database-backed admin review queue. Why: the ML model is retrained separately and the app only needs a demo-safe feedback record. How to reverse: remove DiagnosisFeedback and the feedback endpoints.
T4 | Chose a simple monitoring-plan entity with interval days and crop metadata, rather than a full scheduler or SMS provider. Rejected: real reminder delivery services. Why: the backlog requires a demoable tracking process without external credentials. How to reverse: remove MonitoringPlan and the monitoring endpoints.
T5 | Chose live Open-Meteo-backed weather fetch with graceful fallback to cached/default district data when the API is unreachable. Rejected: synthetic weather generation or hard-coded reports. Why: real weather source is available and the app should still function offline/off-service. How to reverse: remove the weather fallback logic and restore static default values.
T6 | Chose a notification queue with in-memory storage and pluggable sender interface rather than an external push provider. Rejected: Twilio/Firebase credentials injection. Why: the environment does not provide credentials and the app must still demo notifications. How to reverse: remove NotificationQueue and the sender abstraction.
T7 | Chose a status-based escalation workflow with farmer and expert states in the same model, instead of a sprawling workflow engine. Rejected: role-heavy custom workflow engine and cross-service orchestration. Why: a minimal status machine satisfies the demo without overengineering. How to reverse: remove escalation status fields and routes.
T8 | Chose a referral entity that tracks sample status and result, without connecting to a lab system. Rejected: lab integration and physician API dependency. Why: the task only needs a sample-tracking lifecycle for the demo. How to reverse: remove Referral and sample-tracking endpoints.
T9 | Chose village-level aggregated hotspot data with a privacy-safe map model rather than exact geolocation exposure. Rejected: exact farm pinning in public map views. Why: privacy and demo requirements prefer safe aggregate reporting. How to reverse: remove hotspot aggregation and map endpoints.
T10 | Chose lightweight trap and sensor ingestion records with thresholds and alerts, rather than a full IoT ingestion pipeline. Rejected: proprietary sensor gateway and calibration service. Why: the backlog needs representative data capture, not live hardware. How to reverse: remove trap and sensor entities and the threshold logic.
T11 | Chose CSV export and district trend summaries built on persisted records rather than a full BI stack. Rejected: external reporting service. Why: a static export is enough for the demo and keeps the app operational. How to reverse: remove export endpoints and trend aggregation.
T12 | Chose KVK fallback recommendation text and region-aware advisory rules without inventing product dosages or PHI/REI claims. Rejected: any pesticide/product-specific dosage or legal safety statement. Why: the app must not invent restricted or regulated recommendations. How to reverse: remove KVK fallback logic and restore the existing advisory text only.
T13 | Chose image-quality and crop-mismatch gating logic that blocks uncertain uploads rather than attempting unsupported diagnosis. Rejected: synthetic model confidence tuning. Why: the model accuracy is separate and the app must fail safely. How to reverse: remove validation gates and restore direct diagnosis submission.
T14 | Chose client-side local queuing and service worker support without a backend sync engine. Rejected: full offline replication and conflict-synchronization platform. Why: the app is a field-friendly demo and not a full distributed offline system. How to reverse: remove queued sync storage and service worker registration.
T15 | Chose an audit trail and explicit consent placeholders with a minimal governance model rather than a legal platform. Rejected: full consent-compliance productization. Why: the task requires auditability and consent capture, not external legal tooling. How to reverse: delete audit trail and consent entities.
T16 | Chose a seed-data loader that hydrates realistic demo records in one command, rather than an external migration tool. Rejected: custom DB migration framework. Why: the app needs a repeatable demo seed without extra operational tooling. How to reverse: remove the seed-data loader and data script.
